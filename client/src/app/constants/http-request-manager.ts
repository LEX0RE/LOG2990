import { environment } from 'src/environments/environment';

export const GET_DICTIONARY = environment.serverUrl + '/dictionaries/';
export const GET_TURN_TIMES = environment.serverUrl + '/turnTimes';
export const BEST_SCORES = environment.serverUrl + '/bestScores/';
export const GET_NAME = environment.serverUrl + '/virtualPlayerName/';
export const HISTORY_URL = environment.serverUrl + '/gameHistory';
